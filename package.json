{
  "name": "hry-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "portal",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n portal,server -c blue,green \"npm run dev -w portal\" \"npm run dev -w server\"",
    "build": "npm run build -w shared && npm run build -w portal && npm run build -w server",
    "test": "npm test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
